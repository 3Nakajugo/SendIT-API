language: python
python:
  - "3.8.7"
# command to install dependencies
install:
  - pip install -r requirements.txt
script:
  - coverage
  - coveralls
  - pytest --cov=app/
  # coverage run --source=. manage.py test
after_script:
  - coveralls
